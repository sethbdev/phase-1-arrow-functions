const divide = () => {
    let a = 2000
    let b = 100
    return a / b;
}

const square = (param) => {
    let x = 2;
    return (param) * (param);
}

const add = (a, b) => {
    return (a) + (b);
}